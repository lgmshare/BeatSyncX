<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BeatSyncX"
        tools:targetApi="31">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.BeatSyncX.NoActionBar.Translucent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.EditActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.BeatSyncX.NoActionBar"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.ArticleDetailActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.BeatSyncX.NoActionBar" />

        <activity
            android:name=".ui.PrivacyActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.BeatSyncX.NoActionBar" />

    </application>

</manifest>